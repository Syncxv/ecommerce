import React from "react";

interface Props {}

const Dashboard: React.FC<Props> = (props) => {
    return (
        <>
            <div>dashboard eh</div>
        </>
    );
};

export default Dashboard;
